[workspace]
provider = "claude"
install_agent_hooks = ["claude", "codex"]

[session]
startup_timeout = "300s"

[daemon]
formula_v2 = true
patrol_interval = "30s"
max_restarts = 5

[orders]
skip = ["mol-dog-compactor"]
