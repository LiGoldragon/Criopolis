# Vocabulary

*Speech-to-text mistypes Li's project names predictably. Read this
silently — fix the spelling on read, never echo a mistype back to
Li (that reinforces the error).*

## Authoritative spellings

- **criome** = `c-r-i-o-m-e`. Frequent mistypes: "creom", "creom OS",
  "creo me". Read as criome / criome OS / criome's.
- **lojix** = `l-o-j-i-x` (live spelling on disk: `lojix-cli`,
  `lojix-cli-v2` at `/home/li/git/`). Li dictates this as "logic" /
  "logix" / "lojix"; speech-to-text varies. There is no `logix`
  repo. Authoritative: **lojix**.
- **sema** = `s-e-m-a`.
- **Criopolis** = `c-r-i-o-p-o-l-i-s`. Li's name for this gas city
  itself; the city on which the council sits. Speech-to-text
  mistypes: "creopolis" / "creo polis" / "crepolis" / "Korea
  Polish". Always read as Criopolis.
- **CriomOS** = `C-r-i-o-m-O-S`. The criome operating-system axis.
  Speech-to-text mistypes: "Creom OS" / "Crio mosque" / "crime os".
- **keel** — the future engineering-guidelines repo at
  `github.com/LiGoldragon/keel`. Currently wiped; the council will
  rewrite. Mistype: "kiel".

## Council vs. forum

**Council** replaces **forum** in Li's vocabulary going forward
(May 2026). The five seats (satya / viveka / dharma / prayoga /
rasa) plus mayor are now "the high council." Existing seat prompts
still say "forum"; defer the rename until Li signals it. Use
"council" in your own voice and new artifacts.

## Active criome ecosystem (per explorer brief 2026-05-03)

Live components — when Li says any of these, treat as the on-disk
name unless context disambiguates:

- **`home`** → `CriomOS-home` (Home Manager profile)
- **`horizon`** → `horizon-rs` (typed schema + projection lib/CLI)
- **`library`** → context-dependent: `CriomOS-lib` / `mentci-lib` /
  `arca`'s reader. *No plain `library` repo on disk.*
- **`workspace`** → `~/git/workspace/` (meta-repo with manifest)
- **`lore`** → `~/git/lore/` (operational docs)

When Li dictates a component name and you can't tell which repo,
ask once.

## How to apply

- Silent fix on read; do not echo the mistype.
- If uncertain whether Li meant the project term or another sense,
  ask once.
- Maintain this list over time; if Li dictates a new project name
  with a unique spelling, add it here.
