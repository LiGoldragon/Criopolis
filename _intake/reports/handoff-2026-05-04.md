# Mayor handoff — 2026-05-04

*Filed before context compaction. Read this first if you're the next mayor session.*

## Where we are

Today's session crossed a real architectural inflection point. The cascade-test-1 council round (multi-agent patterns research review) produced excellent outputs but **demonstrated that prompt-driven cascade discipline is structurally unreliable**: 5 of 5 agents skipped `--notify` in some form. That finding drove an architectural pivot:

- **Majordomo dropped.** Was attempting to be the always-on cascade receiver via mail-discipline. Doesn't survive prompt-discipline failure. Replaced by an external orchestrator daemon (in build).
- **Cascade orchestration moves to the database.** The bead store is the cascade record. An external Rust daemon subscribes to gc events (`bead.created`, `bead.closed`), reads cascade metadata, dispatches transitions, mails mayor on chain completion. No agent-side cascade discipline.
- **Mayor's role narrows back to editorial.** No babysitting. Sling the council, wait for closes, synthesize.

## Architectural decisions this session

1. **Drop majordomo.** Done. Session closed (`gc session close majordomo`), prompt template deleted, pack.toml entry removed, gc reload confirmed gone.
2. **Build orchestrator as a Criome-style Rust repository.** Code-writer slung on `cr-qp7bha` to implement. Stack: Rust + redb (embedded state) + rkyv (zero-copy serialization) + Criome coding style (per `~/git/lore/programming/*.md`). Repo: `LiGoldragon/orchestrator` on GitHub (code-writer creates it themselves via `gh CLI`).
3. **Cascade orchestration via gc events, NOT Stop hooks.** Stop hooks rejected as too heavy (per-session shell startup overhead). The events are already in the database; orchestrator subscribes via `gc events --since <cursor>`.
4. **Cascade composition lives in bead metadata.** No separate file. Each chain bead carries `cascade_id`, `cascade_position`, `cascade_next`, `cascade_final`, label `cascade-chain`. Mayor creates beads; orchestrator slings everything (including the first agent on `bead.created` with `cascade_position=1`).
5. **Cleanup of mail/cascade-discipline noise from prompts.** Removed from all 10 agent prompts (5 seats + 4 tools + mayor). 509 lines deleted. Cascade-tester intentionally untouched (still useful for end-to-end orchestrator testing).
6. **Operating-rules clean.** `_intake/operating-rules/agents.md` and `mayor.md` had no cascade-discipline references; nothing to remove there.

## In flight

| Bead | Agent | Status | Output |
|---|---|---|---|
| `cr-qp7bha` | code-writer | slung, working | Build orchestrator as Criome-style Rust repository; commit per logical change; push as he goes; create LiGoldragon/orchestrator on GitHub himself |
| `cr-36e8kl` | researcher | slung, queued behind cr-607sxs which closed | tmux usability for Colemak / ex-terminal users; output → `research/answers/tmux-usability.md` |
| `cr-7h6obh` | code-writer | closed earlier today (commit fee073d) | Shell daemon sketch + `orders/cascade-health.toml` — superseded by Rust orchestrator (cr-qp7bha) but kept as reference |

## Open beads (mayor decisions deferred)

- `cr-ohkbvw` — tmux UX: detach behavior + scrollback ergonomics. P3, deferred per Li until tmux usability research returns.
- `cr-w7ea` — staging-city setup script. From prior session, blocked on §8 (mayor doesn't author scripts); should reroute to Prayoga or code-writer when Li directs.
- `cr-y0fm` — auditor design route. Mid-flight from prior session; Prayoga next when Li directs.
- `cr-1g56` — design test-pilot role prompt (after staging city stands up). P2, deferred.
- `cr-6szw` — subsidy interface design. P2, deferred.
- `cr-44st` — synthesizer role decision (round 9 or later). P2, deferred.
- Several other P2 / P3 mayor-design beads from prior synthesis-012 work — see `gc bd list --priority p2,p3 --status open`.

## Vocabulary entries to add (Li's speech-to-text mangles these)

Add these to `_intake/operating-rules/vocabulary.md` next time the file is touched:

- **redb** = `r-e-d-b`. Rust embedded database (https://github.com/cberner/redb). Speech-to-text mangles as: "ekyb", "EDB", "r k y b". Used by Criome ecosystem; orchestrator uses it for cursor/state persistence.
- **rkyv** = `r-k-y-v`. Rust zero-copy deserialization framework (https://github.com/rkyv/rkyv). Speech-to-text mangles as: "ACTR", "actor" (when context is serialization, not actor systems). Used by Criome.

Also confirmed:
- Li uses Colemak keyboard layout (not QWERTY). Affects tmux prefix-key recommendations.

## Mail use-cases that REMAIN valid (kept in prompts)

While cleaning up cascade-discipline mail noise, I confirmed these mail use-cases stay:

- **Mayor → seat sharper-follow-up mail** (push-back during deliberation). In mayor's prompt as `gc mail send <seat> -s "..." -m "..."`.
- **General agent-to-agent communication** (FYI, status questions). Standard `gc mail send` without `--notify` discipline.
- **`gc mail send --notify mayor`** at end of cascade chain. The orchestrator does this; agents don't.
- **`gc mail send --notify` for one-off alerts.** Researcher / code-writer surfacing blockers to mayor.

What was REMOVED: the per-bead-close ritual `bd update + bd close + gc mail send --notify majordomo` that we know fails 5/5 times.

## Git state

Local main is **3 commits ahead of origin** (per `git log @{u}..HEAD`):

- `acd8461` — Cascade research bundle: canonical pattern, infrastructure feasibility, prior art
- `5c71f31` — Researcher: multi-agent patterns survey corpus (cascade-test-1)
- `76d3874` — Synthesis-013 + orchestrator design v1-v3 (cascade-test-1 close-out)

Plus the prompt-cleanup work is uncommitted (just done, ~509 deletions across 10 files).

**Push at session end** per mayor.md §2. Remember to also commit the prompt cleanup before pushing.

## Reports filed (open in markdown viewer, not chat)

- `_intake/synthesis-013-multi-agent-patterns.md` — council synthesis (mayor's stance + 16-item recommendations table)
- `_intake/reports/orchestrator-design.md` — v1 (Stop-hook based, superseded)
- `_intake/reports/orchestrator-design-v2.md` — v2 (database events, no prior art)
- `_intake/reports/orchestrator-design-v3.md` — v3 (FINAL — integrates explorer prior art; gc convoy control --serve shape; Kit's patrol-agent pattern; cursor + body-filter)
- `_intake/explorer/cr-fc8go4-orchestrator-pattern.md` — explorer's prior-art survey
- `research/answers/cascade-canonical-pattern.md` — researcher cr-kbg2ir (canonical gas-city pattern)
- `research/answers/gc-cascade-infrastructure.md` — researcher cr-zsyawg (filter feasibility)
- `research/answers/bead-commit-linkage.md` — researcher cr-607sxs (commit automation patterns)
- `research/answers/multi-agent-patterns/00-overview.md` through `07-criopolis-self-comparison.md` — researcher cr-urcqp0's main corpus

## Immediate next steps for next-mayor

1. **Commit the prompt cleanup** that just happened (509 deletions, 20 insertions across 10 files). Then push the 3+1 commits.
2. **Watch for code-writer to close `cr-qp7bha`** (Rust orchestrator). When done, mayor wires the orchestrator into pack.toml (the one part code-writer can't touch per §8). Then test with cascade-tester chain.
3. **Watch for researcher to close `cr-36e8kl`** (tmux usability). When done, ingest findings; potentially file follow-up bead for tmux config implementation.
4. **Read `research/answers/bead-commit-linkage.md`** when convenient — researcher closed cr-607sxs but I haven't read the output yet.
5. **Decide: cascade-tester future.** Once orchestrator is tested end-to-end, cascade-tester agent can be deleted from pack.toml (no longer needed).
6. **Decide: cleanup of `agents/orchestrator/start.sh` + `orders/cascade-health.toml`.** These are the shell-daemon sketch from cr-7h6obh; superseded by Rust orchestrator. Keep until Rust version is proven, then delete.

## Active sessions at handoff

- `mayor` (always, claude) — this session, about to be compacted
- `control-dispatcher` (always, gc-internal) — long-running

All on-demand sessions are reserved-unmaterialized.

## Notes for the next session

- **Li values reports over chat.** Keep substantive content in `_intake/reports/<topic>.md` files. Tight chat replies pointing to reports.
- **Li uses Colemak.** Keyboard ergonomics matter (tmux prefix etc.).
- **Li doesn't want mayor babysitting cascades.** That's the orchestrator's job. If a cascade fails, report and pause — don't manually push it through.
- **Li corrected several of my over-engineered designs this session.** Watch for: anchoring on LLM-agent shapes when a script suffices; over-engineering with extra stacks (Stop hooks, mail relays); proposing orders/scripts when database events are already paying for the signal.
- **No opinions about whether features are needed** — Li has been explicit on this. Build what's asked; surface specific architectural concerns when they're load-bearing; don't editorialize.
- **Tmux UX is painful for Li.** Avoid complex tmux scrollback dependencies; surface insights via files and reports rather than long terminal output.

## On context compaction

Mayor (Claude Code) cannot compact its own context. Compaction is a harness feature — `/compact` slash command (manual) or auto-triggered near token limits. Li runs the compaction; this handoff is what survives across.

---

**End of handoff.**
